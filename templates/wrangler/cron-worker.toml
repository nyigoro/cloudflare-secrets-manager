# Scheduled Cloudflare Worker config.
# Copy to wrangler.toml and change the Worker name and schedule.

name = "my-scheduled-worker"
main = "src/index.js"
compatibility_date = "2026-04-23"

[vars]
ENVIRONMENT = "production"

[triggers]
crons = ["*/15 * * * *"]
