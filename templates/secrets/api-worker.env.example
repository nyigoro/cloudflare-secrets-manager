# API Worker secrets.
# Copy to production.env or staging.env.

API_TOKEN=replace-with-provider-api-token
WEBHOOK_SIGNING_SECRET=replace-with-webhook-signing-secret
JWT_SECRET=replace-with-long-random-jwt-secret
