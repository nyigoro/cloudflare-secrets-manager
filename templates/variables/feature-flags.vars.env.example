# Plain-text feature flag variables.
# Keep private rollout rules and tokens in secrets, not variables.

FEATURE_SIGNUPS_ENABLED=true
FEATURE_BETA_DASHBOARD=false
FEATURE_VERBOSE_HEALTH=false
FEATURE_READONLY_MODE=false
